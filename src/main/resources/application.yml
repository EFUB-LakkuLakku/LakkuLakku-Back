spring:
  datasource:
    config:
      use-legacy-processing: true
      profiles: local
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/EFUB?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Kjy990216!

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
